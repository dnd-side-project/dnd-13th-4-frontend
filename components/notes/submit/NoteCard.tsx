import { StyleSheet, View } from 'react-native';

const NoteCard = () => {
  return <View style={styles.container}>카드</View>;
};

export default NoteCard;

const styles = StyleSheet.create({
  container: {},
});
